# MoMaJohnPlus 遊戲設計規格

> 狀態：Planned / 尚未實作
> 本文件定義 MoMaJohnPlus 的目標設計。Phase 0 的可執行 Gameplay 仍是原始 MoMaJohn baseline；除非另有註明，下列 Plus 規則尚未進入程式。

## 1. 名詞定義

- **場**：玩家從開始遊戲到 `GAME OVER` 的完整一次遊戲。
- **局**：場中的單次摸麻將牌局。
- **局中事件**：局內抽到事件牌後觸發的事件。
- **局中事件池**：所有局中事件的集合。
- **場中事件**：每局開始前的三選一系統。

場中事件分為裝備抽取事件、場中下注事件、場中特殊事件。

## 2. 核心目標

玩家唯一主要目標是提升最終「分數」。MoMaJohnPlus 未來所有對外顯示用語，會將原本的「點數」統一改為「分數」。這項調整排定於 Phase 1；Phase 0 的現有遊戲畫面仍保留原始用語。

## 3. 場與局

局與局之間的牌局狀態互相獨立。

每局重新建立：手牌、牌池、聽牌、連線、本局場中下注、本局場中特殊效果、本局局中事件狀態。

跨局保留：總分、剩餘局數、裝備。

裝備可以跨局持有，但裝備產生的效果不得延遲到下一局。每一局開始時重新讀取目前持有裝備並建立該局效果。禁止設計跨局 pending effect，例如「本局抽到三張國字，下一局多抽一張」。

## 4. 場中事件

每局開始前，從場中事件池抽出 3 個選項。三個選項可以是相同類型，可以重複出現以前出現過的事件，不要求固定由裝備、下注、特殊各出現一個。

玩家可以暫時選定其中一張；只有真正按下「開牌局」時，選擇才 Commit。

## 5. 開局準備畫面

同一畫面依序包含：上方 3 張場中事件卡、中間槓桿選擇、下方開牌局按鈕。

按下「開牌局」前，玩家可以重新選擇場中事件與槓桿。按下後才確認場中事件、裝備、槓桿、剩餘局數、本局倍率、本局牌數及本局特殊規則。

## 6. 槓桿

| 槓桿 | 消耗局數 |
|---|---:|
| ×1 | 1 局 |
| ×2 | 2 局 |
| ×3 | 3 局 |

剩餘局數不足時不能選擇對應槓桿。場中特殊事件最多只能提供 ×2，因此最高倍率自然限制為 `×3 槓桿 × ×2 特殊事件 = ×6`。禁止新增會造成超過 ×6 的倍率來源；不可依賴結算時截斷成 6，事件設計本身必須守住上限。

允許倍率為 ×1、×2、×3、×4、×6，實作時需維持與現有程式場景及倍率顯示機制相容。

## 7. 正式牌數與小遊戲節點

- 標準正式牌數：15 張。
- 最少正式牌數：14 張。
- 最多正式牌數：16 張。
- 第 8 張：小遊戲節點。
- 第 13 張：小遊戲節點。
- 最後一張必須永遠是普通抽牌，不得是小遊戲。

海底撈月改為「本局最後一張正式牌完成第一條連線」。因此 14、15、16 張局分別在第 14、15、16 張判定。

## 8. 小遊戲池

未來小遊戲：彈珠台、九宮格、記憶力、野球盤、娃娃機、小賭馬、刮刮樂。

Phase 1 與 Phase 2 前期暫不真正實作遊戲內容，但第 8、13 張必須具備進入小遊戲動畫、小遊戲名稱顯示、Placeholder 流程、自動決定取得牌、獲得麻將牌動畫及返回主牌局。第 8、13 張原則上不能使用相同小遊戲。

## 9. 裝備池

裝備欄最多 3 格。裝備可以是持續型、自動消耗型或主動消耗型。裝備滿 3 格後取得新裝備時，必須替換現有一件，不能拒收；裝備消耗後消失並空出欄位。

| # | 裝備 | 規則 |
|---:|---|---|
| 1 | 大吉的籤 | 本局好局中事件機率 ++ |
| 2 | 小吉的籤 | 本局好局中事件機率 + |
| 3 | 小凶的籤 | 本局壞局中事件機率 + |
| 4 | 大凶的籤 | 本局壞局中事件機率 ++ |
| 5 | 嗆司Maker | 本局第一次聽牌時，聽牌獎勵額外 +5 |
| 6 | 喝完的飲料杯 | 沒有任何效果，只佔一格裝備欄 |
| 7 | 免洗護身符 | 自動抵銷取得後第一次壞局中事件，發動後消失；必須可以抵銷「隔壁瓦斯桶爆炸」 |
| 8 | 口袋中的發 | 本局主動使用，指定國字牌為「發」 |
| 9 | 口袋中的中 | 本局主動使用，指定國字牌為「中」 |
| 10 | 口袋中的白板 | 本局主動使用，指定國字牌為「白板」 |

口袋系列可在本局主動使用，將場上一張牌替換成指定國字牌。若場上已存在該牌則不可使用。成功替換後，本局剩餘牌池不得再抽到該牌，且裝備立即消失。

## 10. 場中下注事件池

| # | 事件 | 成功條件 | 成功 | 失敗 |
|---:|---|---|---:|---:|
| 1 | 相信國聚 | 本局取得全部 7 張國字牌：東、南、西、北、中、發、白 | +30 | -30 |
| 2 | 吉一卡哇 | 一萬＋一筒＋一條 | +15 | -10 |
| 3 | 無三不成禮 | 三萬＋三筒＋三條 | +15 | -10 |
| 4 | 五告讚 | 五萬＋五筒＋五條 | +15 | -10 |
| 5 | 字齊七七 | 七萬＋七筒＋七條 | +15 | -10 |
| 6 | 指南針是你？ | 東＋南＋西＋北 | +15 | -10 |
| 7 | 我一定聽牌 | 本局至少一次聽牌 | +5 | -5 |
| 8 | 我一定連線 | 本局至少成功一條連線 | +15 | -5 |
| 9 | 點到為止 | 本局曾經聽牌，但該聽牌線最後沒有完成 | +5 | -5 |

目前分數為 V1 暫定。未來小遊戲加入玩家操作後，必須依實際成功率重新平衡。單次直接得分原則最高為 +30。

## 11. 場中特殊事件池

| # | 事件 | 效果 |
|---:|---|---|
| 1 | 與老闆猜拳 | 立即猜拳；勝：本局分數 ×2，敗：本局分數 ×0.5 |
| 2 | 老闆加碼 | 本局分數 ×2，純正面事件 |
| 3 | 今天牌比較多 | 本局正式牌數由 15 改為 16 |
| 4 | 槓上老闆 | 本局正式牌數由 15 改為 14，且本局分數 ×2 |
| 5 | 大開天眼 | 本局第一張牌由玩家自行決定 |
| 6 | 珠珠寶貝 | 本局第 13 張固定為彈珠台，第 8 張不得為彈珠台 |
| 7 | 當地球隊贏球 | 本局第 13 張固定為九宮格，第 8 張不得為九宮格 |

即使真正小遊戲尚未實作，珠珠寶貝與當地球隊贏球也要在後續 Phase 先建立規則，並由 Placeholder 流程執行。

## 12. 局中事件池分類

### 好事件

- 老闆今天心情很好
- 老闆算錯錢
- 老闆偷偷放水
- 夜市廣播抽中你
- 神秘紅包
- 老闆請你喝飲料
- 神秘大獎
- 口袋發現上次的點數
- 隔壁攤不玩了
- 免費再來一次
- 夜市神明保佑
- 命運逆轉

### 壞事件

- 手滑掉進水溝
- 老闆抓到你偷看
- 隔壁小屁孩哭著求你給他點數
- 小偷來了
- 隔壁高手來亂
- 不揪被抓到
- 隔壁瓦斯桶爆炸
- 故意不小心
- 停電

### 中立事件

- 五條誤
- 偷天換日
- 隔壁棒球攤的球飛過來

「老闆突然加碼」從局中事件池移除，未來改為場中特殊事件「老闆加碼」。

大吉／小吉／小凶／大凶只影響局中事件池中的好事件與壞事件，不影響中立事件、場中事件、裝備抽取、下注事件或特殊事件。

## 13. 分數命名規則

Phase 1 將原始 MoMaJohn 對外 UI 與 Gameplay 文案中的「點數」全面統一為「分數」，例如「總點數 → 總分數」、「本局點數 → 本局分數」、「獲得點數 → 獲得分數」。

程式內部 variable 是否同步 rename，必須在 Phase 1 實作前評估風險；不為文字統一而強迫進行大規模、無必要的內部 rename。
